# AI Vision — environment variables
# Copy this file to .env and fill in your values.
# .env is git-ignored — never commit real keys.

# Existing backend base URL (e.g. Firebase Cloud Function HTTP entrypoint)
# Example:
# BACKEND_URL=https://us-central1-your-project.cloudfunctions.net/api
BACKEND_URL=
